# Face Recognition using a Custom Pretrained Model
This project focuses on the exquisite ability of python to process and recognize faces as images and match them with the corresponding names

## Installation 
To install all the dependencies for this project

run 
```sh
    pip install -r requirements.txt
```
at the root of the project in your command line 